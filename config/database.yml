postgres: &postgres
  adapter: postgresql
  encoding: unicode
  timeout: 10_000
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: <%= ENV['DATABASE_HOST'] %>
  username: <%= ENV['DATABASE_USERNAME'] %>
  password: <%= ENV['DATABASE_PASSWORD'] %>
  port: <%= ENV['DATABASE_PORT'] %>

development:
  primary:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_development" %>
  queue:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_development_queue" %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_development_cable" %>
    migrations_paths: db/cable_migrate
  cache:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_development_cache" %>
    migrations_paths: db/cache_migrate

test:
  primary:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_test" %>
  queue:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_test_queue" %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_test_cable" %>
    migrations_paths: db/cable_migrate
  cache:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_test_cache" %>
    migrations_paths: db/cache_migrate

staging:
  primary:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_staging" %>
  queue:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_staging_queue" %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_staging_cable" %>
    migrations_paths: db/cable_migrate
  cache:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_staging_cache" %>
    migrations_paths: db/cache_migrate

uat:
  primary:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_uat" %>
  queue:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_uat_queue" %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_uat_cable" %>
    migrations_paths: db/cable_migrate
  cache:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_uat_cache" %>
    migrations_paths: db/cache_migrate

production:
  primary:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_production" %>
  queue:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_production_queue" %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_production_cable" %>
    migrations_paths: db/cable_migrate
  cache:
    <<: *postgres
    database: <%= "#{ENV['DATABASE_NAME']}_production_cache" %>
    migrations_paths: db/cache_migrate
